enum Token {
  Int(value: Int)
  Ident(name: String)
  Plus
}

fn token_value(t: Token) -> Int {
  match t {
    Int(v) => v,
    Ident(name) => 0,
    Plus => 0,
  }
}

fn main() -> Int {
  let a := Int(7)
  let b := Plus()
  print(token_value(a))
  print(token_value(b))
  0
}
